// AUTO-GENERATED by protoc-gen-webviewrpc
// (C) "Client" side: HelloServiceClient
// import { WebViewRpcClient } from 'your-lib'; // (선택사항)
import { encodeHelloRequest, decodeHelloResponse } from './hello_world.js';

export class HelloServiceClient {
    /**
     * @param {WebViewRpcClient} rpcClient
     */
    constructor(rpcClient) {
        this.rpcClient = rpcClient;
    }


    // // /**
    // //  * async SayHello
    // //  * @param { HelloRequest } requestObj
    // //  * @returns {Promise< HelloResponse >}
    // //  */
    async SayHello(requestObj) {
        // 1) encode requestObj => Uint8Array
        const reqBytes = encodeHelloRequest(requestObj); // 예: MyMessage.encode(...)
        // 2) rpcClient.callMethod => Promise<Uint8Array>
        const respBytes = await this.rpcClient.callMethod("HelloService.SayHello", reqBytes);
        // 3) decode => responseObj
        const respObj = decodeHelloResponse(respBytes);
        return respObj;
    }

}
